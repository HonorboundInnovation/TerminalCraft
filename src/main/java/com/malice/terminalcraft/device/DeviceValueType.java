package com.malice.terminalcraft.device;

/** Stable structured value kinds exchanged across the device API. */
public enum DeviceValueType {
    NULL,
    STRING,
    NUMBER,
    BOOLEAN,
    LIST,
    MAP
}
